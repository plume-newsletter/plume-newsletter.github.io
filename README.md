# Plume website

Marketing landing page + documentation for [Plume](https://github.com/plume-newsletter/plume),
the open-source self-hosted newsletter platform.

**Status:** not started. This site will be built once the Plume app reaches
feature-complete (likely Astro Starlight — landing + docs in one project), and
deployed independently (e.g. Cloudflare Pages / Vercel).

Part of the `plume-newsletter` org. Deploys separately from the app repo
(`plume-newsletter/plume`).
